# Security Review Report

**Status**: ✅ Complete
**Generated**: 2026-01-31 03:57:36 (UTC)
**Total Issues**: 3

---

**⚠️ CRITICAL INSTRUCTIONS**: For AI agents fixing issues:
1. ✅ After fixing each issue, check off its checkbox below (`- [ ]` → `- [x]`)
2. 📅 Update the status above when all issues are resolved
3. ⛔ DO NOT leave this report without checking completed items

---

## 📊 Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | 1 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## 🔍 Issues

### 🔴 Critical Issues

| # | File | Location | Category | Problem | Suggestion | Rationale | Evidence | References |
|---|------|----------|----------|---------|------------|-----------|----------|------------|
| 1 | .claude/hooks/slack-notify.sh | 32-35, 40-44, 48-52, 67-70, 81-85 | injection | Heredoc 내에서 JSON 입력으로부터 추출된 변수($MESSAGE, $PROJECT_NAME, $SHORT_CWD, $DETAIL_TEXT, $COMMAND 등)가 이스케이프 없이 직접 확장되어 JSON Injection 및 잠재적 Command Injection 가능 | jq를 사용하여 JSON 값을 안전하게 이스케이프하거나, @json 필터를 적용하여 특수 문자를 처리해야 함. 예: MESSAGE=$(echo "$INPUT" \| jq -r ".message // \"\"" \| jq -Rs ".") | OWASP Top 10 A05:2025 Injection - 사용자 제공 데이터가 인터프리터(JSON 파서, 쉘)에 이스케이프 없이 전달되면 공격자가 데이터 구조를 조작하거나 명령을 주입할 수 있음 | 코드 라인 33: {"text":"${MESSAGE:-권한 승인이 필요합니다}"} - MESSAGE 변수에 큰따옴표("), 백슬래시(\\), 개행문자(\n)가 포함되면 JSON 구조가 깨지거나 의도하지 않은 필드가 주입될 수 있음. 예: MESSAGE=test | .claude/skills/owasp-top10-2025/references/A05-injection.md |

### 🟡 Medium Issues

| # | File | Location | Category | Problem | Suggestion | Rationale | Evidence | References |
|---|------|----------|----------|---------|------------|-----------|----------|------------|
| 1 | .claude/hooks/slack-notify.sh | 21 | injection | sed 명령에서 $HOME 변수가 정규표현식 패턴으로 직접 사용되어 정규표현식 인젝션 가능 | $HOME 값에서 정규표현식 특수 문자를 이스케이프하거나, sed 대신 bash의 문자열 치환(${CWD/#$HOME/~}) 사용 권장 | OWASP Top 10 A05:2025 Injection - 변수 값이 정규표현식 컨텍스트에서 이스케이프 없이 사용되면 예기치 않은 패턴 매칭이 발생할 수 있음 | 코드: SHORT_CWD=$(echo "$CWD" \| sed "s\|^$HOME\|~\|") - $HOME에 정규표현식 메타문자(예: ., *, [, ])가 포함된 경우 의도하지 않은 매칭 발생. 일반적인 환경에서는 위험도가 낮으나 방어적 코딩 원칙에 위배됨 | .claude/skills/owasp-top10-2025/references/A05-injection.md |

### 🟢 Low Issues

| # | File | Location | Category | Problem | Suggestion | Rationale | Evidence | References |
|---|------|----------|----------|---------|------------|-----------|----------|------------|
| 1 | .claude/hooks/slack-notify.sh | 25 | security-misconfig | curl 명령의 URL 인자가 프로세스 목록(ps)에서 다른 사용자에게 노출될 수 있음 | 민감한 URL은 환경 변수나 설정 파일에서 직접 읽지 않고, curl의 --config 옵션이나 stdin을 통해 전달하는 것을 고려. 현재 구현은 싱글 유저 환경에서는 수용 가능 | OWASP Top 10 A02:2025 Security Misconfiguration - 민감한 정보가 프로세스 인자로 전달되면 시스템의 다른 사용자에게 노출될 수 있음 | 코드: curl -s -X POST ... "$SLACK_WEBHOOK_URL" - 멀티유저 시스템에서 ps aux 실행 시 웹훅 URL 노출 가능. 단, 이 스크립트는 로컬 개발 환경용 훅으로 보이며 실제 위험도는 배포 환경에 따라 다름 | .claude/skills/owasp-top10-2025/references/A02-security-misconfiguration.md |

---

## ✅ Fix Checklist

**⚠️ MANDATORY**: Check each box (`- [x]`) immediately after fixing the issue.

Track your progress by checking off fixed issues:

- [x] #1 [Critical] .claude/hooks/slack-notify.sh:32-35, 40-44, 48-52, 67-70, 81-85 - Heredoc 내에서 JSON 입력으로부터 추출된 변수($MESSAGE, $PROJECT_... ✅ `jq -n`을 사용한 `build_payload()` 함수로 자동 이스케이프 처리
- [x] #2 [Medium] .claude/hooks/slack-notify.sh:21 - sed 명령에서 $HOME 변수가 정규표현식 패턴으로 직접 사용되어 정규표현식 인젝션 가능 ✅ bash 내장 문자열 치환으로 변경: `${CWD/#$HOME/\~}`
- [x] #3 [Low] .claude/hooks/slack-notify.sh:25 - curl 명령의 URL 인자가 프로세스 목록(ps)에서 다른 사용자에게 노출될 수 있음 ✅ 로컬 개발 환경용 스크립트이므로 허용

**Completion Rule**: When all checkboxes are checked, update the Status at the top to `✅ Complete`.

---

## 📝 Notes

Please address the issues above in order of severity (Critical → Low).

---

*Generated by review-report skill*